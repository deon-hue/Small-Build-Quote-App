import type { ReactNode } from 'react'

export const metadata = { title: 'Get a Quote', description: 'Get an AI-powered building estimate.' }

export default function GetQuoteLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
